<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="admin">

	<resultMap id="memberRes" type="memberModel">
		<result column="MEMBER_ID" property="MEMBER_ID" />
		<result column="MEMBER_PASSWORD" property="MEMBER_PASSWORD" />
		<result column="MEMBER_NAME" property="MEMBER_NAME" />
		<result column="MEMBER_EMAIL" property="MEMBER_EMAIL" />

	</resultMap>

	<!-- 공통으로 사용할 select -->

	<sql id="select_member"> SELECT * FROM MEMBER </sql>
	<select id="memberList" resultMap="memberRes"> SELECT * FROM MEMBER ORDER BY
		MEMBER_NAME DESC </select>

</mapper>